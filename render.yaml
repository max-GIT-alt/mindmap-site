# render.yaml
services:
  - type: static              # <— the key bit
    name: mindmap-site
    plan: free
    buildCommand: ""          # no build step
    staticPublishPath: "."    # root folder contains index.html
