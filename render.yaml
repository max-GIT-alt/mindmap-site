# 100 % static-site service  – simplest
services:
  - type: static           # <- critical: “static”, not “web”
    name: mindmap-site
    plan: free
    buildCommand: ""       # no build step
    staticPublishPath: "." # repo root contains index.html
